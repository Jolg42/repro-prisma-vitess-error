generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider     = "mysql"
  url          = "mysql://root:root@127.0.0.1:33807/reprovitess"
  relationMode = "prisma"
}

model Post {
  id    Int    @id @default(autoincrement())
  title String
}
